[project]
name = "pipeline"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "torch>=2.4.0",
    "torchvision",
    "torchaudio",
    "opencv-python",
    "numpy",
    "pandas",
    "matplotlib",
    "tqdm",
    "psutil",
    "gpustat",
    "librosa",
    "soundfile",
    "scipy",
    "pydub",
    "pyyaml",              # MuseTalk inference config — 3_generate_video.py
    "jiwer",
    "lpips",
    "pytorch-fid",
    "transformers>=4.45.0",
    "tokenizers>=0.20.0",
    "diffusers>=0.30.2",
    "mmpose",
    "mmengine",
    "mmcv-lite>=2.1.0",    # Pure-python build; MuseTalk workers need mmcv==2.1.0 --no-build-isolation (see setup_workers.py)
    "mmdet",
    "accelerate",
    "seaborn",
    "tensorboard",
    "scikit-image",
    "imageio",
    "imageio-ffmpeg",
    "speechbrain>=1.0.0",
    "faster-whisper",
    "pystoi",
    "dtw-python",
    "pesq>=0.0.4",
    "pyloudnorm>=0.2.0",
    "insightface",
    "onnx==1.16.1",
    "onnxruntime-gpu==1.20.1",
    "nvidia-cublas-cu12",
    "nvidia-cudnn-cu12",
    "ml-dtypes==0.4.0",
    "mediapipe==0.10.14",
    "pingouin",
    "scikit-learn",
    "tyro",
    "omegaconf",
    "einops",
    "moviepy",
    "ffmpeg-python",
    "gdown",
    "albumentations",
    "pykalman",
    "lmdb",
    "colorama",
    "gradio_rangeslider",
    "tensorflow",
    "cv2_enumerate_cameras",
    "opennsfw2",
    "protobuf",
    "customtkinter",
    # "pygrabber" — REMOVED: Windows DirectShow wrapper, not installable on Linux
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pipeline"]

[tool.hatch.metadata]
allow-direct-references = true

# CUDA 12.4 PyTorch wheels — RTX 6000 Ada / L40S compatible
[[tool.uv.index]]
name = "pytorch-cu124"
url = "https://download.pytorch.org/whl/cu124"
explicit = true

[tool.uv.sources]
torch      = { index = "pytorch-cu124" }
torchvision = { index = "pytorch-cu124" }
torchaudio  = { index = "pytorch-cu124" }

[tool.uv.extra-build-dependencies]
chumpy      = ["pip"]
xtcocotools = ["numpy"]

[tool.uv]
prerelease = "allow"
required-environments = ["sys_platform == 'linux' and platform_machine == 'x86_64'"]